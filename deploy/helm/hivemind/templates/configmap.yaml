apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hivemind.fullname" . }}-env
  labels:
    {{- include "hivemind.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
